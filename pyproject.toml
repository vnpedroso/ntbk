[project]
name = "ntbk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vnpedroso", email = "npp.vinicius@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    "filelock>=3.20.3",
    "rich>=14.0.0",
]

[project.scripts]
ntbk = "ntbk.cli:ntbk"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
